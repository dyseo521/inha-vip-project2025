name: 'Setup Node Workspace'
description: 'Setup Node.js environment, install dependencies, and build shared types'

inputs:
  node-version:
    description: 'Node.js version to use'
    required: false
    default: '20.x'
  skip-shared-build:
    description: 'Skip building shared types'
    required: false
    default: 'false'

runs:
  using: 'composite'
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ inputs.node-version }}
        cache: 'npm'

    - name: Install all dependencies
      run: npm run install:all
      shell: bash

    - name: Build shared types
      if: ${{ inputs.skip-shared-build != 'true' }}
      run: cd shared && npm run build
      shell: bash
